services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ubc-ocpi-adaptor
    ports:
      - "${PORT:-6001}:6001"
    env_file:
      - .env
    environment:
      # Defaults match UAT; override via .env for local (e.g. NODE_ENV=development, DATABASE_URL).
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=6001
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET:-your-secret-key-change-in-production-min-32-characters}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN:-24h}
      - OCPI_HOST=${OCPI_HOST:-https://uat-bpp-opci-adapter.ubc.nbsl.org.in}
      - BPP_URL=${BPP_URL:-https://uat-bpp-opci-adapter.ubc.nbsl.org.in}
      - ONIX_BPP_PLUGIN_URL=${ONIX_BPP_PLUGIN_URL:-http://onix-bpp-plugin:8002}
      - CDS_BASE_URL=${CDS_BASE_URL:-https://uat-cds.ubc.nbsl.org.in/cds}
      - CDS_API_KEY=${CDS_API_KEY:-your-cds-api-key}
      - EV_CHARGING_UBC_BPP_ID=${EV_CHARGING_UBC_BPP_ID:-uat-bpp-opci-adapter.ubc.nbsl.org.in}
      - EV_CHARGING_UBC_UNIQUE_ID=${EV_CHARGING_UBC_UNIQUE_ID:-76EU8AV7JusA4x7bLLTRLsiPJUmuFXgjoZggE3KV767ZKocKZX9WqH}
      - EV_CHARGING_UBC_BPP_CLIENT_HOST=${EV_CHARGING_UBC_BPP_CLIENT_HOST:-http://host.docker.internal:7082}
      - PRIVATE_KEY=${PRIVATE_KEY:-}
      - SHOULD_SIGN_CALLBACK_REQUESTS=${SHOULD_SIGN_CALLBACK_REQUESTS:-true}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - CLOUD_PROVIDER=${CLOUD_PROVIDER:-local}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:6001/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - ubc-network

  redis-onix-bpp:
    image: redis:alpine
    container_name: redis-onix-bpp
    ports:
      - "6380:6379"
    command: redis-server --requirepass your-redis-password
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "your-redis-password", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - ubc-network

  onix-bpp-plugin:
    image: manendrapalsingh/onix-adapter:v0.9.5
    container_name: onix-bpp-plugin
    ports:
      - "8002:8002"
    volumes:
      - ./onix-adaptor/config/onix-bpp:/app/config
    environment:
      - CONFIG_FILE=/app/config/adapter.yaml
      - REDIS_PASSWORD=your-redis-password
    depends_on:
      app:
        condition: service_started
      redis-onix-bpp:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - ubc-network

  postgres:
    image: postgres:16-alpine
    container_name: ubc-postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - POSTGRES_DB=${POSTGRES_DB:-ubc_ocpi_adaptor}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - ubc-network

volumes:
  postgres_data:
    driver: local

networks:
  ubc-network:
    driver: bridge

